import React from 'react';
import { Text } from 'react-native';
import FramedLayout from './Layouts/Framed';

const Home = () => (
  <FramedLayout>
    <Text>Home</Text>
  </FramedLayout>
);

export default Home;
