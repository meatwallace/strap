#import './models/Todo/todo.graphql'
#import './models/User/user.graphql'

type Query {
  current: User
  author(username: String!): User
  authors: [User]
  todos: [Todo]
  todo(_id: String!): Todo
}
